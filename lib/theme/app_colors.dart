import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF000000); // Preto profundo
  static const Color surface = Color(0xFF1E1E1E);    // Cinza escuro dos cards
  static const Color primary = Color(0xFFFF8C00);    // Laranja vibrante
  static const Color accent = Color(0xFFFFD700);     // Dourado
  static const Color textLight = Color(0xFFFFFFFF);  // Texto branco
  static const Color textDimmed = Color(0xFFAAAAAA); // Texto secundário
}